name: Coverage

on: [push, pull_request]

jobs:
    coverage:
        runs-on: ubuntu-18.04

        env:
          PHP_VERSION: '8.0'
          DB_VERSION: '8'
          LARAVEL_VERSION: '8.*'

        steps:
            - uses: actions/checkout@master

            - name: Run tests for coverage
              run: ./test --php ${{ env.PHP_VERSION }} --laravel ${{ env.LARAVEL_VERSION }} --db ${{ env.DB_VERSION }} --coverage

            - name: Upload coverage
              run: bash <(curl -s https://codecov.io/bash)
